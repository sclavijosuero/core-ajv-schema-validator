{
  "name": "core-ajv-schema-validator",
  "version": "1.0.0",
  "description": "Lightweight package for API testing to validate API responses against plain JSON schemas, Swagger documents, or OpenAPI documents using the Ajv JSON Schema validator. It can be used directly in any project, but also serves as the core engine for schema validation in the cypress-ajv-schema-validator and playwright-ajv-schema-validator.",
  "main": "src/index.js",
  "types": "src/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sclavijosuero/core-ajv-schema-validator.git"
  },
  "keywords": [
    "plugin",
    "ajv",
    "schema",
    "validator",
    "json"
  ],
  "author": "Sebastian Clavijo Suero",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/sclavijosuero/core-ajv-schema-validator/issues"
  },
  "homepage": "https://github.com/sclavijosuero/core-ajv-schema-validator#readme",
  "dependencies": {
    "@types/node": "^22.13.5",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
